using System.Collections.Generic;
using UnityEngine;

public class ObjectDetectionUtils 
{// Start is called before the first frame update
    public static Dictionary<Vector2, TowerPositionData> FindAllTowersInRange(float range)
    {
        return null;
    }
}
