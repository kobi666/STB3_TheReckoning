using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class TestingVectors : MonoBehaviour
{
    // // Start is called before the first frame update
    // public List<GameObject> Targets;
    // public Dictionary<Vector2, GameObject> TargetDistances = new Dictionary<Vector2, GameObject>();
    // private void Start() {
    //     foreach (GameObject GO in Targets) {
    //         TargetDistances.Add(new Vector2(GO.transform.position.x - transform.position.x, GO.transform.position.y - transform.position.y), GO);
    //     }

    //     foreach(KeyValuePair<Vector2,GameObject> t in TargetsPositions) {
            
    //     }
    // }
}
